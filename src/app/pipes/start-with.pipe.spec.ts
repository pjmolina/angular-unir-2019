import { StartWithPipe } from './start-with.pipe';

describe('StartWithPipe', () => {
  it('create an instance', () => {
    const pipe = new StartWithPipe();
    expect(pipe).toBeTruthy();
  });
});
